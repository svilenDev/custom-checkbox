import "./App.scss";
import CheckboxLayouts from "./components/first-row/CheckboxLayout";

function App() {
  return (
    <main className="page-wrapper">
      <CheckboxLayouts />
    </main>
  );
}

export default App;
